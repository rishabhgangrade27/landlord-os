'use client'

// Auto-print removed — it was blacking out the page as soon as it loaded,
// making it look broken. User clicks "Print / Save as PDF" button manually.
export function PrintTrigger() {
  return null
}
